#!/usr/bin/env ruby

# Preview the current React site, including its content inputs and static assets.
Dir.chdir(File.expand_path("..", __dir__)) do
  exec "npm", "run", "dev", "--", "--port", ARGV[0] || ENV.fetch("PORT", "3113")
end
